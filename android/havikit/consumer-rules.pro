# HaviKit ships no reflection-based wire models in the Android module; the pure
# JVM :havikit-core keeps kotlinx.serialization internal to it. No consumer rules
# are required yet — this file exists so the build script reference resolves.
